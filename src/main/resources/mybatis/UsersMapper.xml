<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.dogest.api.dao.UsersMapper">

    <sql id="BaseColumnList">
        id, email, password, qq, wechat, phone, name, sid, room
    </sql>
    <select id="selectByEmail" resultType="cn.dogest.api.model.User" parameterType="java.lang.String">
        SELECT <include refid="BaseColumnList" />
        FROM users WHERE email=#{email} limit 1
    </select>
    <insert id="insertUserInfo" parameterType="cn.dogest.api.model.User">
        INSERT INTO users
        (email, password, qq, wechat, phone, name, sid, room) VALUES
        (#{email}, #{password}, #{qq}, #{wechat}, #{phone}, #{name}, #{sid}, #{room})
    </insert>
</mapper>